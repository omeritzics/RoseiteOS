name: Build Torah OS
on:
  workflow_dispatch:
  push:
    branches: [ main ]

# משתיק את האזהרות ומכריח שימוש ב-Node 24
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

permissions:
  contents: read
  packages: write

jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push OCI Image
        run: |
          # הרצת ה-Vib בתוך קונטיינר עם העברת הרשאות הגישה (config.json)
          docker run --privileged --rm \
            -v .:/recipe \
            -v /home/runner/.docker/config.json:/root/.docker/config.json \
            -w /recipe \
            ghcr.io/vanilla-os/vib:latest \
            build recipe.yml \
            --tag ghcr.io/${{ github.repository_owner }}/torah-os:latest \
            --push

  build-iso:
    needs: build-image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Generate ISO
        run: |
          # בניית ה-ISO מתוך האימג' שהעלינו
          docker run --privileged --rm \
            -v .:/output \
            ghcr.io/vanilla-os/os-constructor:latest \
            ghcr.io/${{ github.repository_owner }}/torah-os:latest

      - name: Upload ISO Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Torah-OS-ISO
          path: ./*.iso
